# Frozen 60–90s SIH demo (Mock backup is the reliability path)

Build: Reload unpacked `apps/extension/dist` after `pnpm build:extension`. Confirm footer `DEV • <sha>` with no unexpected `*` (or explain dirty).

Planner: **MOCK** (primary for reliability). Remote is optional if Gemini is healthy.

Portal: `python3 -m http.server 5173 --directory apps/test-portal`

## PRIMARY (backup-compatible) — target 75s

Reset: fresh tab, Mock mode, overlay closed then toolbar click.

1. Open `http://localhost:5173/scenario-05-privacy.html` (0–5s)
2. Toolbar → overlay. Goal: `Type civic in the notes field` or the page’s public non-secret field. Run. (5–20s)
   - Show compact: looking locally, hidden from AI, no screenshot sent.
3. Open Evidence: screenshot outbound 0 B; Privacy Receipt human lines. (20–30s)
4. Open `http://localhost:5173/scenario-12-high-risk.html`. Goal: `Click Submit permit` / Submit. (30–50s)
   - TRUST MOMENT: confirmation Allow once / Don’t allow. Deny does nothing. This is not ASK_USER.
5. Open `http://localhost:5173/scenario-08-visual-only.html`. Goal that forces local OCR (visual/canvas). (50–75s)
   - Show OCR invoked locally; still 0 B screenshot outbound.

If step 2 field name differs, use the visible labeled public text field. Do not type real secrets.

## Provider-outage line

“Remote AI is advice. If Gemini is down we use the same local observe/protect/validate/act/verify loop with Mock. Failure does not send raw pages or skip confirmation.”

## 10 consecutive runs

Not executed in this agent environment (Side Panel/user-gesture). Human rehearsal on finale hardware; a failure resets the consecutive count. Record PASS/FAIL per run on paper or `scratch/` (gitignored).
